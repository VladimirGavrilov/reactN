 // ListGrB =  dataContry.filter(item => (item.grName == "B")).map( item => (    
    //       <div key={item.id_contry}><span>{item.name_ru}</span> <button onClick={() => console.log(' нажали на кнопку')}>Выбрать</button> </div >   
    //   ) )
    // ListGrC =  dataContry.filter(item => (item.grName == "C")).map( item => (    
    //     <div key={item.id_contry}><span>{item.name_ru}</span> <button onClick={() => console.log(' нажали на кнопку')}>Выбрать</button> </div >   
    // ) )
    // ListGrD =  dataContry.filter(item => (item.grName == "D")).map( item => (    
    //   <div key={item.id_contry}><span>{item.name_ru}</span> <button onClick={() => console.log(' нажали на кнопку')}>Выбрать</button> </div >   
    // ) )
    // ListGrE =  dataContry.filter(item => (item.grName == "E")).map( item => (    
    // <div key={item.id_contry}><span>{item.name_ru}</span> <button onClick={() => console.log(' нажали на кнопку')}>Выбрать</button> </div >   
    // ) )
    // ListGrF =  dataContry.filter(item => (item.grName == "F")).map( item => (    
    //   <div key={item.id_contry}><span>{item.name_ru}</span> <button onClick={() => console.log(' нажали на кнопку')}>Выбрать</button> </div >   
    //   ) )